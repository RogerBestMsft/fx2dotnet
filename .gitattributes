# Auto-detect text files and normalize line endings
* text=auto

# Force LF for shell scripts
*.sh text eol=lf

# Force CRLF for PowerShell scripts
*.ps1 text eol=crlf

# Force LF for Python scripts
*.py text eol=lf
