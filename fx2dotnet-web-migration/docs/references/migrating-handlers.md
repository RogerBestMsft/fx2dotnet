# Migrating Handlers

Reference notes for migrating legacy HTTP handlers.

- Prefer preserving route shape and behavior first.
- Move one handler at a time into minimal ASP.NET Core endpoints or adapter-backed components.
